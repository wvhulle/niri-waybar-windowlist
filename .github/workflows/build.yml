---
name: Build Module

on:
  push:
    branches: [main]
    paths-ignore: ["**/*.md", "**/*.png", "**/*.jpg"]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build-module:
    name: Build Module
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libglib2.0-dev libgtk-3-dev libpulse-dev libpulse-mainloop-glib0

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build with cargo
        run: cargo build --release

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: niri_waybar_windowlist
          path: /home/runner/work/niri_waybar_windowlist/niri_waybar_windowlist/target/release/libniri_waybar_windowlist.so
          retention-days: 45
